{% set version = "0.4.15.20260408" %}

package:
  name: types-colorama
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/t/types-colorama/types_colorama-{{ version }}.tar.gz
  sha256: 9a816657927489463edec1b7b47933b73fe737d37a3616bf596b7de843441032

build:
  number: 0
  noarch: python
  script:
    - {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools
  run:
    - python >={{ python_min }}

test:
  requires:
    - pip
    - python {{ python_min }}
  commands:
    - pip check
    - test -f $SP_DIR/colorama-stubs/__init__.pyi

about:
  home: https://github.com/python/typeshed
  summary: Typing stubs for colorama
  license: Apache-2.0 AND MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - fhoehle
    - conda-forge/mypy
